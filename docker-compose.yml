version: "3"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"

  frontend:
    image: node:18
    working_dir: /app
    volumes:
      - ./frontend:/app
    command: npm install && npm run start
    ports:
      - "3000:3000"
